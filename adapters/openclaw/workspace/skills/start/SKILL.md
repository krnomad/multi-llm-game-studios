<!-- Generated by tools/projectors/generate_openclaw.py. Do not edit by hand. -->
# start

- User entrypoint: `start`
- Interactive capture mode: `structured_choice`

## Intent

Guide early-session onboarding by detecting project state, collecting the user's current starting point, routing to an appropriate next workflow, and requiring explicit user choice before handoff.

## Required inputs

- user_starting_point

## Workflow steps

- `detect_state` — Silently inspect project context to tailor onboarding recommendations. (confirmation: False)
- `capture_starting_point` — Present starting-point options and capture the user's self-assessment. (confirmation: True)
- `route_path` — Produce a recommended workflow path based on user input and detected state. (confirmation: False)
- `confirm_next_step` — Confirm user-selected next action before handoff and avoid auto-execution. (confirmation: True)

## Required capabilities

- read
- glob
- grep
- user_prompt_capture
- structured_output

## Outputs

- selected_next_action
- routed_workflow_path
- onboarding_rationale

## OpenClaw fallback notes

- Use wrapper-based structured choices in repo or CLI interaction surfaces. (workaround `generated_wrapper`)

## Compatibility warnings

- `W_INTERACTIVE_CAPTURE_WRAPPED` via `generated_wrapper` for `interactive_capture`
- `W_SLASH_COMMAND_ROUTED` via `protocol_downgrade` for `slash_commands`
- `W_APPROVAL_PROTOCOL_ENFORCED` via `native_mapping` for `approval_before_write_protocol`
